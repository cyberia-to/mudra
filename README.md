# mudra

cryptographic primitives for [[cyber]]. seven modules — each proves a property:
confidentiality (seal, stealth, veil), distribution (quorum),
delay (delay), ordering (order), position (place)
